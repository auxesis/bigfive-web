[tools]
bun = "latest"
node = "latest"
"npm:vercel" = "latest"
supabase = "latest"

[env]
NEXT_PUBLIC_ENV = "development"

# Supabase credentials: set in mise.local.toml
NEXT_PUBLIC_SUPABASE_URL = { required = true, redact = true }
NEXT_PUBLIC_SUPABASE_SECRET_KEY = { required = true, redact = true }

[tasks."dev"]
dir = "web"
run = "yarn run dev"

[tasks."db:migrate"]
dir = "web"
run = "supabase migration up"

[tasks."test"]
dir = "web"
run = """
bun run lint
bun run format
bun run test
"""

[tasks."build"]
dir = "web"
run = """
bunx contentlayer2 build
bun run build
"""

[tasks."setup"]
dir = "web"
run = """
bun install
"""
